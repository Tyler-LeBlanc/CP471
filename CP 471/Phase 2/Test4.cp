int l;
gcd(12,15);

def double newFunc(int o)
	o = 15;
fed

def int func(int c, double e)
	int a;
	o = 10;
	if(l = 0) then
		print(l);
	fi;
fed

def int gcd(int a, int b)
	double c , d , e;
	if(a==b) then
		return (a) 
	fi;
	if(a>b) then
		return(gcd(a-b,b))
	else 
		return(gcd(a,b-a)) 
	fi;
	l = 10;
	o = 15;
fed;
print gcd(21,15).